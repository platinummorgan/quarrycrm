# Database
# Local development
DATABASE_URL="postgresql://username:password@localhost:5432/quarry_crm?schema=public"

# Production (Vercel Postgres, Neon, Supabase, or Railway)
# DATABASE_URL="postgresql://user:pass@host:5432/db?sslmode=require"

# NextAuth.js (Required for authentication)
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here-minimum-32-characters"
NEXTAUTH_URL="http://localhost:3000"

# Demo Authentication (Required for demo.quarrycrm.com)
DEMO_TOKEN_SECRET="your-demo-token-secret-here"

# Email Server Configuration (Required for magic link authentication)
# For local development, use a service like Mailtrap (https://mailtrap.io) or Ethereal Email
EMAIL_SERVER_HOST="smtp.mailtrap.io"
EMAIL_SERVER_PORT="2525"
EMAIL_SERVER_USER="your-mailtrap-username"
EMAIL_SERVER_PASSWORD="your-mailtrap-password"
EMAIL_SERVER_SECURE="false"
EMAIL_FROM="noreply@yourdomain.com"

# Environment
NODE_ENV="development"
NEXT_PUBLIC_APP_ENV="development"

# Optional: Disable Next.js telemetry
NEXT_TELEMETRY_DISABLED="1"

# ============================================
# VERCEL DEPLOYMENT NOTES
# ============================================
# 
# When deploying to Vercel:
# 1. Set DATABASE_URL to your production database with SSL
# 2. Generate new NEXTAUTH_SECRET for production
# 3. Update NEXTAUTH_URL to your Vercel domain
# 4. Set NEXT_PUBLIC_APP_ENV appropriately:
#    - "prod" for production (quarrycrm.com)
#    - "preview" for demo (demo.quarrycrm.com) - enables noindex for robots
#    - "development" for local dev
# 5. Set DEMO_TOKEN_SECRET for demo authentication
#
# Example production values:
# DATABASE_URL="postgresql://user:pass@db.example.com:5432/crm?sslmode=require"
# NEXTAUTH_SECRET="<generate-with-openssl-rand-base64-32>"
# NEXTAUTH_URL="https://quarrycrm.com"
# NEXT_PUBLIC_APP_ENV="prod"
#
# Example demo values:
# NEXTAUTH_URL="https://demo.quarrycrm.com"
# NEXT_PUBLIC_APP_ENV="preview"
# DEMO_TOKEN_SECRET="<generate-secure-random-string>"
#
# See docs/vercel-deployment.md for full guide
